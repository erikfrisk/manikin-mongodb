{
  "name": "manikin-mongodb",
  "description": "Data model abstractions for MongoDB",
  "version": "0.12.5",
  "main": "./lib/manikin-mongo.js",
  "author": "Jakob Mattsson <jakob.mattsson@gmail.com> (jakobmattsson.se)",
  "license" : "MIT",
  "repository": {
    "type": "git",
    "url": "http://github.com/jakobmattsson/manikin-mongodb.git"
  },
  "engines": {
    "node": ">= 0.8 < 0.12",
    "npm": ">= 1.3 < 2"
  },
  "dependencies": {
    "manikin-tools": "^0.2.0",
    "mongoose": "^3.6.16",
    "underscore": "^1.4.0",
    "async": "^0.2.0"
  },
  "devDependencies": {
    "runr": "^0.4.3",
    "mocha-lcov-reporter": "^0.0.1",
    "coveralls": "^2.1.0",
    "manikin": "^0.3.0",
    "jscov": "^0.5.1",
    "mocha-term-cov-reporter": "^0.1.3",
    "mongojs": "^0.9.6",
    "mocha": "^1.7.4",
    "coffee-script": "^1.4.0",
    "should": "^1.2.1"
  },
  "scripts": {
    "pretest": "coffee -co lib src",
    "prepublish": "coffee -co lib src",
    "test": "mocha --compilers coffee:coffee-script --recursive spec && npm run travis-coverall",
    "coverage": "jscov src .cov && JSCOV=.cov mocha --compilers coffee:coffee-script --recursive --reporter mocha-term-cov-reporter spec",
    "coverall": "jscov src .cov && JSCOV=.cov mocha --compilers coffee:coffee-script --recursive --reporter mocha-lcov-reporter spec",
    "travis-coverall": "if [ -n \"$TRAVIS\" ]; then npm run coverall | coveralls src; fi",
    "prestart": "coffee -co lib src"
  }
}
